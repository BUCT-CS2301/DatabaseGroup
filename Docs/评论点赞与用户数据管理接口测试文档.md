# 评论点赞与用户数据管理接口测试文档

## 测试范围

- `GET /api/v1/artifacts/{artifactId}/comments`
- `POST /api/v1/artifacts/{artifactId}/comments`
- `POST /api/v1/comments/{commentId}/likes`
- `POST /api/v1/artifacts/{artifactId}/likes`
- `DELETE /api/v1/artifacts/{artifactId}/likes`
- `GET /api/v1/users/{username}/comments`
- `GET /api/v1/users/{username}/likes`
- `GET /api/v1/users/{username}/favorites`
- `GET /api/v1/users/{username}/history`
- `POST /api/v1/users/{username}/history`

## 测试环境

- 日期：2026-06-01
- 后端：Spring Boot，测试端口 `18080`
- 数据库：MySQL `admin_platform`
- 认证：`GET /api/v1/auth/mock-token?userId=admin-user-1&userType=ADMIN`
- 测试用户：`admin-user-1 / admin`
- 测试文物：`artifact-test-1 / 测试青铜鼎`

## 数据库准备

已补充并执行扩展表脚本：

- `docker/mysql/interaction_tables.sql`
- `docker/docker-compose.dev.yml` 已挂载为 `03-interaction_tables.sql`

本地旧库缺少部分基础表，测试时额外补齐了 `museum`、`artifact`、`ugc_comment`、`artifact_image`、`sensitive_word`、`system_log`、`backup_record` 等启动所需表，并插入最小测试数据。

## 连通性测试结果

| 步骤 | 请求 | 结果 |
|------|------|------|
| 1 | `GET /api/v1/artifacts/artifact-test-1/comments?page=1&size=20` | `200 success`，返回 `total=0` |
| 2 | `POST /api/v1/artifacts/artifact-test-1/comments` | `200 success`，创建评论 `868f6ebb-41a1-4f8f-93c8-07436e18ea42`，状态 `APPROVED` |
| 3 | `POST /api/v1/comments/{commentId}/likes` | `200 success`，返回 `likeCount=1` |
| 4 | `POST /api/v1/artifacts/artifact-test-1/likes` | `200 success`，返回 `likeCount=1,isLiked=true` |
| 5 | `DELETE /api/v1/artifacts/artifact-test-1/likes` | `200 success`，返回 `likeCount=0,isLiked=false` |
| 6 | `GET /api/v1/users/admin/comments?page=1&size=20` | `200 success`，返回 `total=1` |
| 7 | `GET /api/v1/users/admin/likes?page=1&size=20` | `200 success`，返回 `total=0` |
| 8 | `GET /api/v1/users/admin/favorites?page=1&size=20` | `200 success`，返回 `total=0` |
| 9 | `POST /api/v1/users/admin/history` | `200 success`，记录 `artifact-test-1` 浏览历史 |
| 10 | `GET /api/v1/users/admin/history?page=1&size=20` | `200 success`，返回 `total=1` |

## 点赞响应字段复测

`POST /api/v1/artifacts/artifact-test-1/likes`

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "likeCount": 1,
    "isLiked": true
  }
}
```

`DELETE /api/v1/artifacts/artifact-test-1/likes`

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "likeCount": 0,
    "isLiked": false
  }
}
```

## 结论

评论、评论点赞、文物点赞/取消点赞、我的评论、我的点赞、我的收藏、浏览历史查询与记录接口均已完成本地连通性测试，接口返回结构符合需求文档约定。
