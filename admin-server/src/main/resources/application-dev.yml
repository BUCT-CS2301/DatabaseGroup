# dev：未接登录时可调用 GET /api/v1/auth/mock-token 获取联调 JWT（app.auth.mock-token.enabled=true 时注册）
app:
  auth:
    mock-token:
      enabled: true
    trust-mock-token-claims: true
  relics:
    # 本地联调用对外基址，与 artifact_image.file_name 拼接为完整图片 URL
    image-public-base-url: "http://localhost:8080"
spring:
  datasource:
    url: jdbc:mysql://39.106.231.119:3306/admin_platform?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: 643114514
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: 39.106.231.119
    port: 6379
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
